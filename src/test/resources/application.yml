spring:
# 설정을 적지 않으면 기본적으로 memory 모드로 db를 가짐
#  datasource:
#    url: jdbc:h2:mem:test # memory 모드 h2 DB
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace